#!/usr/bin/bash

file="$(grep -v -e '^$' -e '^.*#' "$HOME/Dropbox/Backup/dotfiles/configfiles.txt" | while read -r line; do
[[ $line =~ \*$ ]] && find -L ${line%\*} -type f -printf '%p\n'|| echo $line
done | fzf)"
[[ -n $file ]] && nvim $file
